interface:
  display_name: "THU Digitizer"
  short_description: "Extract and validate chart data from images, PDFs, and source data"
  default_prompt: "Use $thu-digitizer to extract verified chart data with the registered deterministic implementation; preserve CSV, report, overlay, provenance, and refusal status, and validate against official source data only as a separate step."
